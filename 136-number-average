public Decimal average(List<Decimal> numbers){
    // your code here
    integer a = numbers.size();
    decimal sum = 0;
    for(i=1;i<=a; i++)
    {
        sum = sum[i]/i;
    }
    return setScale(sum,2);
}

