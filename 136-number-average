public Decimal average(List<Decimal> numbers){
    // your code here
    Integer a = numbers.size();
    decimal sum = 0;
    for(Integer i=0;i<a; i++)
    {
       sum = sum + numbers[i];
       sum = sum/a;
    }
    return sum;
}

