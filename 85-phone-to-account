public Map<String, Account> phoneToAccount(List<Account> accounts) {
    //code here
    Map<String, Account> nMap = new Map<String, Account>();
    for(Account acc: accounts)
    {
        if(acc.phone != null)
        {
            nMap.put(acc.phone, acc);
        }
    }
    return nMap;
}