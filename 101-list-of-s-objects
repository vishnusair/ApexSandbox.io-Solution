public List<sObject> getListOfsObject(List<Account> accounts, List<Contact> contacts)
{
    // code here
    List<sObject> s1 = new List<sObject>();
    if(accounts?.size() == null && contacts?.size() == null)
    {
        return '';
    }
    return s1;
}