public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if(acc.Id == null)
    {
        return false;
    }
    else if(acc.Id == con.Id == opp.Id)
    {
        return true;
    }
    else
    {
        return false;
    }

}