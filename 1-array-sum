public static Integer arraySum(List<Integer> numbers) {
    //code here
    Integer sum = 0;
    Integer a = numbers.size();
    for(Integer i=0;i<a;i++)
    {
        sum = sum + numbers[i];
    }
    return sum;
}