public boolean isSorted(List<Integer> numbers) {
    //code here
    Integer a = numbers.size();
      If(a<=2)
      {
          return true;
      }  
    for(Integer i=0;i<a-1;i++)
    {
      else
      {
          numbers.sort();
          return true;
      }
    }
    return false;
}