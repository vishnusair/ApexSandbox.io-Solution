public boolean isSorted(List<Integer> numbers) {
    //code here
    Integer a = numbers.size();
    for(Integer i=0;i<a;i++)
    {
      If(a<3)
      {
          return true;
      }  
      else
      {
          numbers.sort();
          return true;
      }
    }
    return false;
}