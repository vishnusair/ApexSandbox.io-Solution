public Boolean isBirthday(Contact c) {
    //code here
    Date a = System.today();
    if(c.Birthdate.Month() == a.Month())
    {
        if(c.Birthdate.Day() == a.Day())
        {
        return true;
        }
    }
    return false;
}